import AddRoomClient from "@/components/clientComponents/AddRoomClient";

export const metadata = {
  title: "Add Room | Study Nook",
};

const AddRoomPage = () => {
  return <AddRoomClient />;
};

export default AddRoomPage;
